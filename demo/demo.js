addEventListener('DOMContentLoaded', function () {

	pickmeup('#fecha-init-riego', {
		position       : 'up',
		hide_on_select : true
	});

	pickmeup('#fecha-end-riego', {
		position       : 'up',
		hide_on_select : true
	});


});
